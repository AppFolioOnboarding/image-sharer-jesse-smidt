<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <style>
      /* Email styles need to be inline */
      body{
        max-width: 600px;
        margin: auto;
        font-family: "Helvetica Neue", Helvetica, Arial, Sans-Serif;
        text-align: center;
        }

      img{
        width: 400px;
        margin: 50px 100px;
      }

      a{
        font-size: 24px;
        margin: 15px 0px;
      }
    </style>
  </head>

  <body>
    <h1>You've been invited to Image Sharer!</h1>
    <h3><%= @share.email %> has shared an image with you!</h3>
    <img src="<%= @image.url %>"/>
    <hr/>
    <% if @share.message.present? %>
      <p>They also included this message:</p>
      <q><%= @share.message %></q>
    <hr>
    <% end %>
    <a href="<%= @linkback_url %>">Check out Image Sharer</a>
  </body>
</html>
